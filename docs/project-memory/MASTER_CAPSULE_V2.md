# MASTER_CAPSULE_V2 — ПРО Спокойствие Website

> Канонический документ проекта (website). Создан: 2026-06-19.
> Основание: фактическое состояние кода + CAPSULE V2 VS CODE AUDIT.
> **Источник истины = код.** `MASTER_CAPSULE.md` (2026-06-07) считается **V1/устаревшим**.
>
> Легенда тегов:
> ✅ CONFIRMED — подтверждено кодом · ⚠️ VERIFY — требует проверки владельцем ·
> ⚖️ LEGAL — юридический риск · 🗑 OUTDATED — устарело (из V1) · 🚫 DO-NOT — не делать.

---

## 1. Project identity
- Клиника: Медицинский центр «ПРО спокойствие», Екатеринбург. ✅
- Юрлицо: ООО «РОМИС-В» · ИНН 6686168326 · ОГРН 1256600028970. ✅
- Лицензия: №Л041-01021-66/04064884, Министерство здравоохранения Свердловской области. ✅
- Телефон: +7 (343) 345-49-05 · Telegram @Procalmnessekb · e-mail prospokoystvie@mail.ru. ✅(в коде) / ⚠️ номер (V1 отмечал двойственность 345-49-05 ↔ 345-59-05).
- Будущая доменная почта заявок: info@pro-spokoystvie.ru. ⚠️ домен/ящик не активны.
- Профиль: психиатрия, психотерапия, психология, неврология. Позиционирование — спокойное, конфиденциальное, без постановки на учёт. ✅

## 2. Current repo/code status
- Remote: `origin → github.com/druzhbacompany/pro-spokoystvie-site`. ✅
- Branch: `main`. ✅
- Последний commit: `054808d Add booking direction selector` (предыдущие: SMT footer hotfix, branches, conversion logic, SMT rebuild). ✅
- Незакоммиченные изменения: `app/api/lead/route.ts`, `package.json`, `package-lock.json`, `.env.example` (работа по email-лидам ещё не в git → не задеплоено). ⚠️

## 3. Actual stack
- **Next.js 14.2.15, App Router.** ✅ (🗑 V1 говорил «Tilda» — устарело.)
- React 18.3.1 · TypeScript 5.5.3 · Tailwind 3.4.10 · `resend ^6.14.0`. ✅
- Хостинг: **Vercel** (целевой). ⚠️ деплой-конфиг в репо не зафиксирован, подтвердить проект в Vercel.
- Scripts: `dev`, `build`, `start`, `lint`, `typecheck`. ✅

## 4. Actual routes
Публичные (SMT, пререндерятся): `/`, `/uslugi`, `/uslugi/[slug]`, `/vrachi`, `/vrachi/[slug]`, `/vrachi/romanovsky-vo`, `/tseny`, `/dokumenty`, `/kontakty`, `/o-klinike`, `/programmy`, `/programmy/[slug]`, `/thanks`. ✅
API: `/api/lead` (dynamic). ✅
SSG-параметры: услуги → `trevozhnoe-rasstroistvo`, `panicheskie-ataki`; врачи → 5 slug; программы → 4 slug. ✅
Legacy (noindex, вне навигации): `/hero-v2`, `/hero-v3`, `/hero-v4`, `/old-home`. ⚠️ требуют решения (удалить/оставить).

## 5. Actual data layer
Единый источник `lib/data.ts`: ✅
- `NAV` — 7 пунктов: Услуги · Программы · Врачи · Цены · Документы · О клинике · Контакты.
- `DOCTORS` — 6 · `SERVICES` — 9 (детальные страницы только у 2) · `SERVICE_CATEGORIES` — 4.
- `PROGRAMS` — 4 · `BRANCHES` — 2 · `DOCUMENTS` — 4 · `PRICELIST` — 6 разделов / 80 услуг + `PRICELIST_FILE`.
- Хелперы: `bySlug`, `serviceBySlug`, `programBySlug`, `branchById`.
- 🚫 DO-NOT: хардкодить врачей в формах/компонентах вне `DOCTORS` (состав персонала меняется).

## 6. Product canon
- Тематическая маршрутизация (по состоянию), НЕ аудиторная (взрослые/дети). ✅
- Направления (категории): Тревога и стресс · Настроение и состояние · Поддержка и восстановление · Неврология. ✅
- Сервис-страницы собраны для тревоги и паники; остальные направления — карточки каталога → запись по телефону/форме. ✅
- Программы — 4 placeholder-уровня (состав уточняется), без мед-гарантий. ✅ / ⚠️ контент.

## 7. Business canon
- Запись = главная конверсия; единая форма-якорь `#zayavka` на каждой странице + телефон. ✅
- Оплата: наличные и карты (на странице цен). ✅
- 🗑 OUTDATED (V1): выручкообразующий контур «наркология/кодирование», декларации «15 лет / 1000+ клиентов / 75%» — в коде НЕ используются (стоят плейсхолдеры «Данные уточняются»). Не возвращать без подтверждения. ⚠️

## 8. Legal/public wording canon
- Публичное позиционирование — анти-нагнетание; запрещены «гарантия», «100%», «24/7», «круглосуточно», «вывод из запоя», «нарколог» в маркетинговой копии. ✅ (в коде чисто).
- ⚖️ LEGAL: на `/tseny/` публично виден прайс с чувствительными позициями из утверждённого DOCX: «Эспераль», «Лазерное кодирование», «Метод Торпедо», «Снятие кода», «Наркотическая зависимость», «Детокс» (IV). Владелец одобрил «в таблице», но публичный показ требует sign-off по ФЗ-38.
- IV-терапия и процедуры — формулировки «по назначению врача / по медицинским показаниям». ✅
- 🚫 DO-NOT: выносить нарко-/кодирование-термины в hero/навигацию/мета/позиционирование.

## 9. UX canon
- Один спокойный канал записи (форма + телефон + Telegram-ссылка), без мульти-чат-зоопарка. ✅
- Каждая карточка услуги имеет рабочее действие: есть страница → «Подробнее»; нет → «Записаться» с контекстом. ✅
- Crosslink врач↔услуга, строка прайса→форма (контекст), филиал→форма (контекст). ✅
- Breadcrumbs на вложенных, `/thanks` после заявки. ✅

## 10. Visual SMT-like canon
- Канон — **SMT-like** (на PRO-контенте; donor-DNA blue/beige отменён владельцем). ✅
- Токены `.smt` в `app/globals.css`: blue `#4895cd`, dark `#00354b`, radius 15/30/40, тень `0 10px 40px #168dcd33`, контейнер 1200, Manrope; футер — `.smt-footer` (plain CSS, purge-proof). ✅
- Компоненты `components/smt/*`: SiteHeader, SiteFooter, Breadcrumbs, Placeholder, BookingForm, Cta, DoctorCard, ServiceCard, PriceTable, DocumentsGallery, DoctorProfile, Branches. ✅
- Все 13 публичных страниц используют SMT. Legacy V1/V2/V4-компоненты — только для noindex `/old-home` и `/hero-v*`. ✅
- 🚫 DO-NOT: не возвращать старый pine/green и blue/beige donor-DNA на публичные страницы без решения владельца.

## 11. Forms and lead handling
- `components/smt/BookingForm.tsx`: видимое опциональное поле **«С чем хотите обратиться?»** (direction). ✅
- 🚫 Нет выбора врача (`doctor` selector / `preferredDoctor` отсутствуют). ✅
- Контекст в POST `/api/lead`: name, phone, time, topic, doctor (из URL-контекста), direction, priceItem, price, branch, branchAddress, sourceBlock, ctaLabel, pageUrl, pageTitle, consent, company(honeypot). ✅
- Валидация: имя+телефон+согласие обязательны; honeypot игнорируется. ✅
- direction предзаполняется из контекста услуги/прайса (best-effort). ✅

## 12. Email/Resend status
- `/api/lead` читает env: `RESEND_API_KEY`, `LEAD_EMAIL_TO` (fallback info@pro-spokoystvie.ru), `LEAD_EMAIL_FROM` (fallback noreply@pro-spokoystvie.ru). ✅
- Если ключ есть → письмо через Resend (`mode:"email"`); нет → серверный лог `mode:"log-only"`; ошибка → `mode:"email-failed-logged"` (форма не падает). ✅
- **Сейчас: log-only** (ключа нет; работа не закоммичена/не задеплоена). ⚠️
- Telegram-лиды — **отложены** (только ссылка-контакт, не канал доставки). ✅
- `.env.example` присутствует (имена без значений). ✅ · `docs/project-memory/EMAIL_LEADS_SETUP.md` — инструкция активации. ✅
- 🚫 DO-NOT: печатать значения секретов; добавлять Telegram-доставку без решения.

## 13. Branches
- `BRANCHES` = 2: **kosmonavtov** — «Филиал на Космонавтов», г. Екатеринбург, пр-кт Космонавтов, 101Б (основной); **gromova** — «Филиал на Громова», г. Екатеринбург, ул. Громова, 30. ✅
- Контакты общие для филиалов. ✅
- Карта: непустая SMT-панель + ссылки Яндекс/маршрут; Громова — ссылка-поиск по адресу. ⚠️ реальные координаты/embed не verified (координаты НЕ выдумывать — 🚫).
- Филиалы показаны на `/`, `/kontakty/`, `/o-klinike/`, в футере; карточки → `/kontakty/?branch=<id>#zayavka`. ✅

## 14. Doctors
6 врачей (✅ в `DOCTORS`):
| Slug | Имя | Роль | Фото |
|---|---|---|---|
| romanovsky-vo | Романовский В.О. | Главный врач | temp |
| bezyazykova-iv | Безъязыкова И.В. | специалист | нет (плейсхолдер) |
| selkina-eo | Селькина Е.О. | специалист | есть |
| franko-ov | Франко О.В. | специалист | нет (плейсхолдер) |
| dvornikova-ea | Дворникова Е.А. | специалист | есть |
| tadevosyan-ns | Тадевосян Н.С. | специалист | нет (плейсхолдер) |
- 🗑 OUTDATED (V1): Санников О.С., Иванова О.В. — НЕ в составе (per OWNER_DECISIONS). 🚫 не добавлять без решения.
- ⚠️ VERIFY: фото 3 врачей, стаж/образование/награды/отзывы/видео — плейсхолдеры «готовится/уточняется».

## 15. Prices
- `PRICELIST` — 6 разделов / 80 услуг, дословно из утверждённого DOCX. ✅
- Скачивание полного прайса: `PRICELIST_FILE` (`/documents/preyskurant-pro-spokoystvie.docx`, в git через `.gitignore`-исключение). ✅
- Строка прайса кликабельна → форма с контекстом priceItem; раздел имеет CTA. ✅
- ⚖️ LEGAL: разделы «Лечение зависимостей» (03) и IV-терапия публично видимы — нужен sign-off (см. §8).

## 16. Documents
- `/dokumenty/` — `DocumentsGallery`: 4 реальных скана (лицензия, правила приёма, график, контролирующие органы) в **модальном окне** (lightbox, ESC/клик-вне/кнопка). ✅
- 🚫 На карточках документов НЕТ «Скачать» и НЕТ открытия в новой вкладке. (Скачивание DOCX есть только для прайса на `/tseny/`.) ✅

## 17. Skills
Проектные `.claude/skills/`: brand, code-review-skill, design, design-system-pro, minimalist-skill, pro-design-autopilot, redesign-skill, soft-skill, taste-skill, ui-styling, ui-ux-pro-max. + `.claude/commands/design-autopilot.md`, `.claude/launch.json`. ✅
- 🚫 DO-NOT: менять `~/.claude` (глобальные настройки).

## 18. Quality gates
- `npm run typecheck` ✅ · `npm run lint` ✅ · `npm run build` ✅ (29 страниц, `/api/lead` dynamic). Подтверждено аудитом 2026-06-19.
- Перед сдачей: typecheck+lint+build зелёные; нет `href="#"`; формы шлют контекст; модалка документов работает; mobile-меню полное; нет SMT-ассетов/контента.
- ⚠️ `npm install resend` сообщил о 5 уязвимостях в дереве зависимостей (не чинились; `audit fix --force` НЕ запускать без решения).

## 19. Known risks
1. ⚖️ Публичный показ нарко-/IV-позиций в прайсе (ФЗ-38).
2. ⚠️ Email не активен (log-only) до `RESEND_API_KEY` + верификации домена.
3. ⚠️ Email-работа не закоммичена → не в проде.
4. 🗑 `MASTER_CAPSULE.md` (V1) противоречит коду (Tilda/наркология/Санников/цифры).
5. ⚠️ Номер телефона (345-49-05 ↔ 345-59-05).
6. ⚠️ Плейсхолдеры: фото врачей/интерьер, программы, цифры клиники.
7. ⚠️ npm audit: 5 уязвимостей.
8. ⚠️ Legacy `/hero-v*` + `/old-home` в проде (noindex).

## 20. Open owner decisions
1. Публиковать ли весь прайс публично (включая раздел зависимостей и IV)? ⚖️
2. Подтвердить телефон. ⚠️
3. Коммит + активация email (Resend ключ, верификация домена `pro-spokoystvie.ru`, ящик info@). ⚠️
4. Удалить или закрыть legacy `/hero-v*` и `/old-home`? ⚠️
5. Приоритет следующих сервис-страниц (депрессия/бессонница/ОКР/…). ⚠️
6. Контент программ; данные/фото врачей и интерьера. ⚠️
7. Добавлять ли Telegram/иные каналы лидов помимо email? ⚠️

## 21. What Claude must not do (🚫)
- Не менять код/маршруты/данные/`package.json`/`.env` без явного разрешения.
- Не коммитить/не пушить без разрешения.
- Не печатать значения секретов.
- Не менять `~/.claude`; не включать hooks/MCP/фоновые демоны.
- Не ставить зависимости / не запускать install-скрипты / не `audit fix --force` без разрешения.
- Не добавлять выбор врача в форму; не хардкодить врачей.
- Не выносить нарко-/кодирование-/гарантийные формулировки в позиционирование.
- Не выдумывать координаты карты / verified мед-факты / цифры.
- Не удалять старую капсулу `MASTER_CAPSULE.md` без отдельного разрешения.

## 22. Next tasks (после одобрения)
1. Owner sign-off по публичному прайсу → при отказе скрыть/гейтить разделы 03 и IV.
2. Закоммитить email-лиды; добавить `RESEND_API_KEY` + верифицировать домен в Vercel.
3. Архивировать V1: переместить `MASTER_CAPSULE.md` → `docs/project-memory/archive/` (по разрешению).
4. Подтвердить телефон; заменить плейсхолдеры по мере поступления ассетов.
5. Решение по legacy-маршрутам (удалить/noindex-гейт).
6. Отдельный pass по `npm audit`.
