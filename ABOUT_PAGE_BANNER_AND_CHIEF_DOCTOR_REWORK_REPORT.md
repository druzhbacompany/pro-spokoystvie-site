# ABOUT PAGE — BANNER & CHIEF DOCTOR REWORK REPORT

**Date:** 2026-06-22
**Status:** COMPLETE — typecheck ✅ · lint ✅ · build ✅ · DOM verified ✅
**Mode:** RUFLO / MINIMAL DIFF

---

## 1. КАКИЕ ФАЙЛЫ ИЗМЕНЕНЫ

- `app/o-klinike/page.tsx` — единственный изменённый файл:
  - hero: безопасная обрезанная версия заменена на **полный баннер**;
  - добавлен блок юридической приписки (SMT-notice);
  - полностью переработан блок «Главный врач»;
  - удалён неиспользуемый импорт `DoctorCard`.

Изображения (`about-banner-original.jpeg`) уже присутствовали в репозитории с прошлой задачи — новые файлы не создавались. `about-banner-safe.jpeg` больше не используется страницей (остаётся в `public/assets/clinic/` как неиспользуемый артефакт; не удалялся ради минимального диффа).

---

## 2. КАКОЙ БАННЕР ТЕПЕРЬ ИСПОЛЬЗУЕТСЯ

**`/assets/clinic/about-banner-original.jpeg`** — полный баннер 1080×1920, **без обрезки**.

Отображается целиком: логотип, «медицинский центр / ПРО спокойствие и здоровье», «Медицинские анализы HELIX», «Приём специалистов», «IV-терапия, детокс-капельницы», «Выполнение назначений врача», «Кодирование от алкогольной зависимости», телефонная плашка — все смысловые блоки сохранены, ничего не вырезано.

Технически: помещён в SMT-карточку (белый фон, скругления, тонкая граница, мягкая тень), `width=1080 height=1920`, `className="h-auto w-full"`, ограничен `max-w-[380px]` и центрирован, чтобы вертикальный баннер не доминировал в hero. Подтверждено через DOM: `about-banner-original`, `complete: true`.

---

## 3. ГДЕ ДОБАВЛЕНА ЮРИДИЧЕСКАЯ ПРИПИСКА

Под hero-блоком (под баннером), в том же `<section>`, отдельным спокойным info-блоком в SMT-стиле:
- скруглённая карточка, белый фон, тонкая граница `var(--smt-border)`;
- слева компактная иконка-кружок «i» (цвет бренда), справа мелкий текст (`12.5px`, `smt-muted`);
- **не** красный/крупный дисклеймер — визуально спокойный notice.

Текст приписки (дословно по ТЗ):
> «Услуги оказываются при наличии медицинских показаний, после консультации специалиста и в рамках действующей лицензии. Поддержка при зависимом поведении и восстановительные процедуры проводятся вне острого состояния, в составе индивидуального плана помощи. Имеются противопоказания, необходима консультация специалиста.»

Запрещённые слова («нарколог», «вывод из запоя», «лечение алкоголизма») в тексте приписки **не использованы**.

---

## 4. КАК ПЕРЕРАБОТАН БЛОК «ГЛАВНЫЙ ВРАЧ»

Было: `DoctorCard` (col-span-1) + текстовая карточка (col-span-2) с большой пустой белой областью справа.

Стало: единая карточка `flex flex-col md:flex-row overflow-hidden`:
- **слева** — фото Романовского (`/assets/doctors/romanovsky-temp.jpg`), `object-cover object-top`; на мобильном — сверху на всю ширину (высота `h-80 / sm:h-96`), на десктопе — колонка `md:w-[300px]`, растягивается на высоту текстовой части (пустая область устранена);
- **справа** — имя (`h3`), затем чипы-факты:
  «Главный врач» (выделен синим), «Врач-психиатр, психотерапевт», «11 лет практики», «Более 1 000 консультаций»;
- короткий текст (дословно по ТЗ): «Романовский Владимир Олегович отвечает за медицинский стандарт клиники и координацию помощи между специалистами. В работе делает акцент на спокойной, бережной и понятной коммуникации с пациентом.»;
- цитата сохранена, оформлена компактно (тонкая левая полоса, курсив);
- кнопка-ссылка «Подробнее о главном враче →» сохранена (прижата к низу через `mt-auto`).

Баланс: карточка `flex-row` — фото и текст одной высоты, без пустот. На мобильном — вертикальный стек. Подтверждено через DOM: карточка 327px высотой, фото 300×325 заполняет левую колонку, все 4 чипа и текст присутствуют.

Глобальный компонент `DoctorCard` и врачи в `lib/data.ts` **не менялись** — правки только в блоке на `/o-klinike`.

---

## 5. РЕЗУЛЬТАТЫ TYPECHECK / LINT / BUILD

```
npx tsc --noEmit   → чисто (нет вывода)
npx next lint      → ✔ No ESLint warnings or errors
npx next build     → ✓ Compiled successfully
                     ○ /o-klinike  (static)
```

---

## 6. РЕЗУЛЬТАТЫ GREP

```
grep -RniE "нарколог|вывод из запоя|лечение алкоголизма|лечение наркомании|100%|гарантия результата" app/o-klinike components lib
```
Совпадения (все — НЕ в публичном тексте /o-klinike, НЕ нарушения):
- `components/v4/HeroV4.tsx:77`, `components/v3/HeroV3.tsx:108` — `100%` в CSS-градиентах (`linear-gradient(... 100%)`), неиспользуемые donor-компоненты, мной не затрагивались.
- `lib/data.ts:107` — комментарий-документация со списком запрещённых слов (не публичный текст).
- `lib/data.ts:193` — комментарий «НЕ включено: …наркологии — REWRITE_REQUIRED» (документация, не публичный текст).

В `app/o-klinike/page.tsx` (изменённый файл) и в добавленной юридической приписке запрещённых формулировок **нет**.

---

## 7. GIT STATUS --SHORT

```
 M app/o-klinike/page.tsx
```
(прочее в рабочем дереве — неотслеживаемые артефакты прошлых задач: `CLINIC_BRANCH_PHOTOS_REPORT.md`, папка-источник «1. Сбор для заполнения сайта/»; данный отчёт добавится как новый untracked-файл.)

---

**STOPPED BEFORE COMMIT. STOPPED BEFORE PUSH.**
